#include <stdio.h>
#include <stdlib.h>

int main()
{
    int choice;

    while (1)
    {
        switch (expression)
        {
        case /* constant-expression */:
            /* code */
            break;

        default:
            break;
        }
    }
}